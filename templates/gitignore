# .NET
bin/
obj/
*.user
*.suo
.vs/
artifacts/
TestResults/
*.db
*.db-shm
*.db-wal

# Node
node_modules/
dist/
coverage/
.vite/
*.local

# Quality gate success stamp (local cache, machine-specific)
.gate-stamp

# Coverage output written by the gate's server leg
.coverage/

# Editors / OS
.idea/
*.swp
.DS_Store
Thumbs.db

# Local-only Claude settings (machine-specific overrides)
.claude/settings.local.json

# Secrets (AZURE_DEVOPS_PAT lives here)
.env
.env.local
